services:
  backend:
    build: ./backend
    ports: ["8000:8000"]
    environment:
      SECRET_KEY: change-me-in-production
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-}
  frontend:
    build: ./frontend
    ports: ["80:80"]
    depends_on: [backend]
